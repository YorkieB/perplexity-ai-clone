/**
 * Server and client may apply this suffix when "photoreal" quality is requested.
 * Keep in sync with `vite-plugins/openai-proxy.ts` (duplicated string) or extract to a shared JSON if needed.
 */
export const PHOTOREAL_PROMPT_SUFFIX =
  ' Ultra photorealistic detail: natural skin texture with visible pores, individual hair strands, soft natural lighting, shallow depth of field, 85mm lens character, high resolution, avoid plastic or waxy skin.'

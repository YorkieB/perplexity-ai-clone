# Required for chat, follow-ups, file analysis, and model council (proxied at /api/llm in dev/preview — key stays server-side).
OPENAI_API_KEY=

# Optional: OpenAI-compatible API base (OpenRouter, Azure, etc.)
# OPENAI_BASE_URL=https://api.openai.com/v1

# Optional: Tavily web search (https://tavily.com)
VITE_TAVILY_API_KEY=
